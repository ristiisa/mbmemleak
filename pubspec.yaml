name: mbmemleak
description: "A new Flutter project."
version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  mapbox_maps_flutter:
    git:
        ref: 0eed5d8cdcd34687a48019699b9f7dd7c8dc465e
        url: https://github.com/mapbox/mapbox-maps-flutter.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true

  assets:
    - path.json